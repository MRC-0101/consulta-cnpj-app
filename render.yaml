services:
  - type: web
    name: consulta-cnpj
    env: python
    plan: free
    branch: main
    buildCommand: python -m pip install -r requirements.txt
    startCommand: gunicorn consulta_cnpj:app
    autoDeploy: true
    runtime: python-3.11
